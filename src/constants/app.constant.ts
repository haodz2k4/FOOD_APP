

export enum Gender {
    MALE = 'male',
    FEMALE = 'female'
}

export const RESPONSE_MESSAGE = "response_message";

export enum SortOrder {
    ASC = "asc",
    DESC = "desc"
}

export enum Status {
    ACTIVE = 'active',
    INACTIVE = 'inactive'
}